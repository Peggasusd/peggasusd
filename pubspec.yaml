name: peggasusd
description: PEGGASUSD - Lightning wallet for Cuba with SAT and USD
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  bip39: ^1.0.6
  flutter_secure_storage: ^9.2.4
  path_provider: ^2.1.5
  logging: ^1.3.0
  intl: ^0.20.2
  provider: ^6.1.2
  breez_sdk_spark_flutter:
    git:
      url: https://github.com/breez/breez-sdk-spark-flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
